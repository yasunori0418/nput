# テストレビュー記録: quality-observability / design

- レビュー対象: `docs/test/quality-observability/test-design.md` /
  `docs/test/quality-observability/test-case.md`
- 実施日: 2026-07-27（計 3 回。初回差し戻し〔must 2 / want 4 / nit 2〕→ 2 回目差し戻し
  〔must 0 / want 3 / nit 1〕→ 3 回目差し戻し〔must 0 / want 1 / nit 1〕）
- 判定: 差し戻し（利用者判定。指摘 2 件を解消してから通過を取る方針）
- 機械検査: OK 10 / NG 0 / SKIP 0（review-check.sh design）

初回・2 回目の指摘（計 12 件）はすべて実質を伴って解消済み。**未解消の指摘は 3 回目で
新規に検出した 2 件のみ**で、いずれも成果物の記録の整合に関するもの。テストの中身
（カバレッジ・検出能力・トレーサビリティ）に欠陥は検出されていない。

3 回目のレビューでは、修正による新規欠陥の混入を重点的に確認し、次の点が棄却されている:
TC-07 の判定点数の変更（10 → 11）は設計書の表・本文の両方へ反映済みで他工程成果物への
波及も無い / 可変分母は `test-monitoring.md` 3 章の未実装メトリクスに関わるもので、
実装済みの `tests/traceability/run.sh` は R# 別の消化率を算出しないため現時点の計測に
実害は無い / CASE-15 の CASE-41 依存は完了基準 1 の分母にも自動・手動の内訳にも影響しない /
実物照合（`modules/*.nix` の素朴走査 7 件に対し規則適用後 5 件・CLI 6 件・lib 公開 6 件・
完成の定義の合計 5 件）はいずれもケースの記述と一致。

## 未解消の指摘

| # | 指摘（何が問題か） | 重み | 解消の条件 |
|---|---|---|---|
| 1 | **成果物の改訂の記録が現状と乖離している**。`test-design.md` は今回 TC-07 のカバレッジ基準を 10 → 11 判定点へ変更する実質的な改訂を受けたが、ヘッダは `作成日: 2026-07-26` のみで改訂の記録を持たない。一方 `test-case.md` は改訂を記録しているものの、その内容が `2026-07-27 改訂: design レビューの指摘 8 件へ対応` と**初回レビュー分のままで、2 回目の指摘 4 件への対応が反映されていない**。カバレッジ基準は完了判定の根拠であり、いつ・なぜ変わったかを成果物単体から追えない状態は、仕様書（`spec.md`）が改訂履歴節を持つ本リポジトリの慣習からも外れる | want | `test-design.md` のヘッダへ 2026-07-27 の改訂（TC-07 のカバレッジ基準を 10 → 11 判定点へ変更）を 1 行記録する。あわせて `test-case.md` の改訂記述を、2 回目の指摘 4 件への対応まで含む形へ更新する |
| 2 | **ケース間依存の記載場所が前提条件欄と補足で不統一**。CASE-18・CASE-19・CASE-20 は期待結果で「CASE-17 の出力と一致する」と CASE-17 へ依存しており、補足にもその旨の記載があるが、**前提条件欄は「健全な作業ツリー」のみ**で依存が書かれていない。同種の依存を持つ CASE-14・CASE-16（および 2 回目の指摘で追加した CASE-15 → CASE-41）はいずれも前提条件欄に明記されており、扱いが非対称になっている。補足に記載があるため test-implement は追えるが、依存の所在が 2 箇所に分かれる | nit | CASE-18・CASE-20 の前提条件欄へ「CASE-17 が実行済み（出力を比較対象に使う）」を追加し、ケース間依存の記載場所を前提条件欄へ統一する（CASE-19 は前提条件欄に実装言語の自己ゲートを持つため、あわせて揃えるかは設計判断） |

## 次のステップ

- `/test-design quality-observability` を再実行し、上記 2 件の指摘へ対応する。いずれも
  **数行の追記で済み、ケースの追加・カバレッジ基準の変更を伴わない**。
- 修正後 `/test-review quality-observability design` を再実行し、指摘なしでの通過を取る。
- 本工程のゲートは未通過のため、mini サマリ（`mini-test-design.md` / `mini-test-case.md`）の
  作成は通過後まで行わない。
