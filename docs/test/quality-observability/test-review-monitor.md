# テストレビュー記録: quality-observability / monitor

- レビュー対象: `docs/test/quality-observability/test-monitoring.md` /
  `tests/traceability/run.sh` / `.github/workflows/test.yml`（`traceability` job）
- 実施日: 2026-07-26（計 4 回。初回差し戻し〔must 3 / want 6 / nit 2〕→ 2 回目差し戻し
  〔want 6 / nit 3〕→ 3 回目 want 1 件を残して修正 → 4 回目で全指摘解消）
- 判定: 通過（利用者判定）
- 機械検査: OK 5 / NG 0 / SKIP 0（review-check.sh monitor）

指摘を受けて是正された主な点:

- **完了基準との紐づけの網羅**（初回 must）— 当初はメトリクスが test-plan 4 章の完了基準
  1・2・3・8 にしか対応しておらず、基準 4〜7・9 が両表から欠落していた。3 章へ 5 行を追加し、
  完了基準 1〜9 すべてが 2 章または 3 章に対応先を持つ状態にした。基準 5 は「空欄 0 件」と
  「該当なし欄がすべて `なし` という文字列」の 2 条件の判定へ改めた。
- **誤紐づけの是正**（初回 must）— NFR カバー率の対応先から基準 3（CI ゲート観測。NFR とは
  無関係）を除去し、基準 7（NFR-01）・基準 1（NFR-03 は R4 経由）へ修正した。
- **実装物と定義文書の矛盾の解消**（初回 must）— 下流成果物が未作成のときの注記が実出力
  （カバー率 0% と全 ID の未カバー列挙）と食い違っていた点を是正し、上流欠落（母数 0 → `—`）と
  下流欠落（0%）の区別も明示した。
- **対象撤去時の CI 赤化の回避**（2 回目 want）— 仕様不在時の終了コードを 2 から 0
  （「計測対象なし」）へ変更した。機能単位の作業ディレクトリは完成後に削除される運用のため、
  撤去のたびに CI が赤くなる状態は「ゲートを持たない」設計と矛盾していた。
- **計測の限界の明記**（2 回目 want）— 範囲表記のリスクを「未カバーの誤検知」から
  「端点マッチによりカバー率が実態より高く出る偽陽性」へ訂正し、参照判定が下流成果物の
  全文突合であること（表の対応 ID 列に限定していないこと）とその限界を明記した。
  前工程成果物 `test-analysis.md` 2 章の注記に残っていた旧説明も同時に是正した。

最終版は、完了基準 9 項目すべてとの対応、取得元の実在性（`spec.md` / `test-plan.md` の
実際の記法と抽出パターンの一致、JUnit XML 形式の出力が存在しないこと）、集計スクリプトの
実挙動と文書記述の一致（正規表現による端点マッチ・終了コードの分岐・母数 0 の扱い）、
既存 CI 構成との整合（`changes` ゲート非適用・`Setup Nix` なし・既存 job 無改変・
required status check 非登録）を、AI 定性レビュー（test-reviewer サブエージェント）が
実物照合で確認済み。

## 次のステップ

- test-monitor へ戻り、mini サマリ（`mini-test-monitoring.md`）を作成して完了宣言する。
- 計測結果の評価が必要になったら `/test-report quality-observability` を実行する。
