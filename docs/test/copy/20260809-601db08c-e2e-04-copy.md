---
id: "CASE-601db08c-2323-48eb-99f5-8774bba2fa6e"
type: test_case
name: "tests/e2e/scenarios/04-copy.sh — 実 nix での copy place-once と out-of-store"
target: "tests/e2e/scenarios/04-copy.sh"
covers:
  - "TC-596d697f-4ba6-4ec1-b71e-8b5375806c08"
  - "TC-cf1b44ec-2ec4-4ee1-9a37-378e41ccb01e"
  - "TC-b1b8c163-9d37-47ee-9838-7168569df03a"
---
# CASE-601db08c: e2e 04-copy

## 対象

`tests/e2e/scenarios/04-copy.sh`

隔離した一時 `$HOME` の下で、`method = "copy"` の entry と `mkOutOfStoreSymlink` の entry を
持つ fixture flake を実 nix で評価し、`nput apply` を一気通貫で回す。

## 主な検証内容

- **copy の配置形**: target が store symlink ではなく実ディレクトリになること、内容が src
  どおりであること、配置されたファイルが書き込み可能であること（read-only な store の
  mode がそのまま残っていないこと。ツリー忠実性のうち e2e が覆うのはこの一点で、mode の
  保存・実行ビット・内部 symlink の非 deref は CASE-82fdfb27 が扱う）
- **out-of-store の live 性**: live ディレクトリを指す symlink が張られること、src 側の
  ファイルを書き換えると target 越しにその内容が見えること
- **place-once の冪等**: 配置済みファイルをローカル編集してから再 apply しても編集が残ること
- **dryrun の change 意味論**: `apply --dryrun --json` で copy entry が change を生まず、
  既存 symlink のみ reversible な再リンク modify として 1 件現れること、items には全 entry が
  success として載ること（フルインベントリ・→ Issue #132）
