---
id: "REQ-706de717-4e47-471a-a1c0-448635be159c"
type: requirement
name: "世代操作は nix-env --profile 系で統一し、GC root の間引きと store 解放を分けて行う"
specification: |
  Every generation of the profile SHALL be a GC root. Thinning out old generations SHALL
  be done through `nix-env --profile <profileDir>/profile --delete-generations <gens>`,
  and the freeing of store paths that have thereby become unreferenced SHALL be done
  through `nix-collect-garbage`. Every generation operation SHALL be unified on the
  `nix-env --profile <profileDir>/profile` family: the commit (`--set`), rollback
  (`--rollback`), switching to an arbitrary generation (`--switch-generation`), listing
  (`--list-generations`) and thinning (`--delete-generations`); only store GC SHALL use
  `nix-collect-garbage`. The new `nix profile` subcommands SHALL NOT be used, being
  incompatible: they require a profile-manifest of their own directly under the profile,
  whereas a profile made by `nix-env --set` has a link farm there, containing nput's
  `manifest.json`.
specification_ja: |
  profile の各世代は GC root でなければならない。旧世代の間引きは
  `nix-env --profile <profileDir>/profile --delete-generations <gens>` で行い、それにより無参照に
  なった store パスの解放は `nix-collect-garbage` で行う。世代操作は
  `nix-env --profile <profileDir>/profile` 系で統一しなければならない（コミット `--set`・
  rollback `--rollback`・任意世代切替 `--switch-generation`・一覧 `--list-generations`・間引き
  `--delete-generations`）。store GC のみ `nix-collect-garbage` を使う。新 `nix profile`
  サブコマンドは使ってはならない（profile 直下に自身の profile-manifest を要求するのに対し、
  `nix-env --set` 製 profile は profile 直下が link-farm であり非互換のため）。
---
# REQ-706de717: 世代操作は nix-env --profile 系で統一し、GC root の間引きと store 解放を分けて行う

## 仕様

profile の各世代は GC root。`nix-env --profile <profileDir>/profile --delete-generations <gens>`
で旧世代を間引き、`nix-collect-garbage` で無参照になった store パスを解放する。

> **世代操作は `nix-env --profile <profileDir>/profile` 系で統一する**。コミット（`--set`）・
> rollback（`--rollback`）・任意世代切替（`--switch-generation`）・一覧（`--list-generations`）・
> 間引き（`--delete-generations`）。store GC のみ `nix-collect-garbage`。
> 新 `nix profile`（`list` / `wipe-history` / `rollback`）は profile 直下に自身の profile-manifest を
> 要求するが、`nix-env --set` 製 profile は profile 直下が link-farm（nput の `manifest.json` を
> 含む）なので **`nix profile` サブコマンドとは非互換**。

> **上は原文の写しで、規範は frontmatter が正**。原文が併記する「可変 JSON 方式は GC root を
> 作らず参照中 store パスが GC で消えるため採らない」は棄却案の理由であり要求ではない
> （manifest を store 内へ埋め込むことは REQ-1be4d678 の担当）。`<profileDir>` のレイアウトは
> REQ-2aa3abbc、`rollback` の手順は REQ-0e341430 の担当。

## 出典

`docs/spec.md`「世代管理仕様」→「GC とストレージ解放」節。

決定の実体は ADR-0015「実装前レビューで surfaced した残セマンティクス」の nix-env 統一と、
`<profileDir>` レイアウトを確定した ADR-0025。
