---
id: "RISK-7808768d-bd87-429a-af42-52e8559d940c"
type: risk
name: "Nix と engine の唯一の契約である manifest の生成側と消費側がずれる"
threatens:
  - "REQ-79ce0a09-f9bd-4e61-ba7f-45fb5643137b"
  - "REQ-250d936c-1df0-491d-a7af-708f38b61f33"
  - "REQ-dec58330-6dad-47f7-8f56-2402764a89c7"
likelihood: low
impact: high
level: medium
---
# RISK-7808768d: Nix と engine の唯一の契約である manifest の生成側と消費側がずれる

## リスク

`manifest.json` は Nix 側の評価と Go 側の engine をつなぐ唯一の安定契約であり、両側が同じ
文書を同じ意味で読めることに全体が乗っている。ここがずれると、評価は成功しビルドも通るのに
配置の段で意味を取り違える、という発見の遅い壊れ方をする。

ずれ方は 2 方向ある。

- **バージョンの skew** — 利用者が pin した flake（生成側）と手元の CLI（消費側）の版が
  食い違い、消費側の知らない版の文書が渡る。このとき「壊れた JSON」ではなく「読めるが意味の
  違う文書」として通ってしまうと、ずれに気づく機会が失われる。移行機構を持たない方針を
  採っている以上、拒否そのものが契約の一部であり、しかも原因が skew だと分かる形で拒否
  できなければ利用者は対処できない
- **契約の暗黙拡張** — 消費側が未知のフィールドを黙って無視すると、生成側が足したつもりの
  情報が届かないまま実行が進む。届いていないことに誰も気づかない

root の種別ごとの扱いも同じ場所にある。評価時に絶対パスが決まる種別と、実行時に解決する種別が
あり、この区別を取り違えると配置先そのものが変わる。

## 影響

意図しない場所への配置、あるいは原因の分からない失敗。impact を high とするのは、この契約が
統合層すべての土台であり、ずれが「読めるが意味の違う文書」として黙って通る形で nput のどの
利用経路にも波及するため。likelihood を low に留めるのは、本 item の射程が消費側の受理
（版の拒否・未知フィールドの拒否）と統合層の配線に限られ、前者は Go の契約テスト
（TC-172548ea）が、後者は activation が root と entry とオプションを engine へ届けることの
評価アサート（TC-5a72b00e）が覆っているため。評価層が文書の形を変えてしまう側は本 item では
なく RISK-5df2d02b の担当で、そちらはフィールド追加・既定値変更という通常の改修で動くぶん
likelihood が一段高い。

ただし覆われているのは上限側（消費側の知らない新しい版の拒否）で、版が下限を下回る場合の
拒否と root 種別が空の場合の拒否は CASE-4179dcb2 が自ら残余として挙げている。
