---
id: "RISK-3de9753f-3fd3-4364-b1ab-64c68c15ec77"
type: risk
name: "評価スイート自身の盲点でアサートが空虚に緑になる"
likelihood: low
impact: high
level: medium
threatens:
  - "DSG-fb49e36c-eb20-4efa-8cac-88ef0873db8e"
---
# RISK-3de9753f: 評価スイート自身の盲点でアサートが空虚に緑になる

## リスク

検証手段を nix-unit（点の不変条件）と namaka（面のスナップショット）の 2 本立てにする設計
（DSG-fb49e36c）には、その組み合わせに固有の盲点が 2 つある。どちらも「スイートが緑なのに
守られていない」という形をとり、他のどのアサートの信頼性も同時に下げる。

**テスト名の後勝ち衝突**: nix-unit のスイートはテストディレクトリの列挙で組み立てられ、
`//` マージが衝突を後勝ちで黙って解決する。テスト名がファイル横断で衝突すると、落ちるはずの
アサートが実行されないまま緑になる。落ちないだけでなく、実行されていないこと自体が出力に
現れない。

**スナップショットへの委ね過ぎ**: namaka のスナップショットは変更時に丸ごと再承認される。
規範である性質をスナップショットだけに委ねると、「変わったこと」は差分として見えても
「変わってよかったのか」は問われないまま承認が通る。

**この破れが design に張る理由**: どちらも 2 手段併用という検証設計の選択に由来し、別の設計
（例: 生成した文書を Go 側の契約テストで直接検証する）へ置き換えれば消える。
`docs/agents/sara-graph.md` の判別規約に従い design 側へ張る。

## 評価

- likelihood: low — 判定を支配するテスト名の衝突側が機械検査で覆われた。leaf ファイルを
  追加するたびに起こりうる衝突を、アグリゲータのマージ前重複検査が評価時に必ず落とす。
  スナップショットへの委ね過ぎは TC-de6514e2 が覆うが、こちらは線引きの規約であって
  機械検査ではない（→「対処」節）。当初は衝突側に機械的な手立てが無く `high` と
  判定していたが、検査の追加でゲートの見逃し幅が消えた
- impact: high — 空虚に緑になったアサートは、それが守っていたはずの不変条件を丸ごと無防備に
  する。しかも失敗ではなく成功として現れるため、気づく契機が無い（silent inconsistency）

## 対処

スナップショットへの委ね過ぎは TC-de6514e2 が緩和する（同 TC が、規範である性質は名前付き
アサート側で守りスナップショットへ委ねない旨を条件として持つ）。

**テスト名の後勝ち衝突はアグリゲータ自身が緩和する**（→ Issue #287）。アグリゲータ
（検査を含むマージの実体は `tests/nix-unit-lib.nix` の `mergeTests`）は `//` マージの前に
ファイル横断の重複キーを検査し、衝突があれば衝突したテスト名と定義元ファイル名を示して
throw する。一意性は TP-36e90d5d が定めるスイート組み立ての規約だが、
その遵守はレビュー任せではなく評価時に機械的に強制される。各 leaf ファイルがファイル固有の
接頭辞（`testEscapesBase*` / `testAnchorName*` / `testResolveMarker*` / `testFarmEntries*` など）を
付ける慣行はそのまま衝突を避ける第一手として残り、`escapes-base.nix` のヘッダコメントが
その意図を明記している。

**その検査自身も TC-539ce047 が緩和する**（→ Issue #308）。実ファイルに衝突が無い限り検査の
throw 側は一度も実行されず、常に「衝突なし」を返す退行が本 risk と同じ形（緑なのに守られて
いない）で再現するため、検査を `tests/nix-unit-lib.nix` の純関数へ切り出してダミー入力で
アサートする。列挙（`builtins.readDir` で `{ file, tests }` を組み立てる側）は同 TC の射程外で、
列挙が leaf を取りこぼす形の破れは残余として本 risk が引き続き持つ。

同種の「テスト自身の検出力の限界」は他にも残る。CASE-879a93da の `expectedError.msg` は部分
一致でゲートの取り違えを検出できず、本 risk と同じ「緑なのに守られていない」形の残余である。

TC-1d69350e の組み立て検証も、現状は `lib/manifest.nix` の生成式を複製した独立コードなので
本体の変更に追随しない。ただしこちらは Issue #289 が `anchorLines` の生成式を `__internal`
seam として切り出し、CASE 対象のテストを共有式参照へ再構成する予定で、完了すれば追随する
ようになる（同 issue の完了時に本節を更新する）。

## 出典

`tests/nix-unit.nix` のアグリゲータ実装（当時は `lib.foldl' (acc: m: acc // m)` の後勝ちマージのみ
で、重複検査は Issue #287 で追加した）と `tests/namaka/` の運用からの逆算（→ Issue #273
「L1〜L4」節）。2 手段併用の設計判断は DSG-fb49e36c、その規約は TP-36e90d5d が持つ。
