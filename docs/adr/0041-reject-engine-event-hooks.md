---
id: "ADR-0041"
type: adr
name: "engine イベント hook スクリプト機構を採用しない（合成 idiom を docs に置く）"
status: 採用
status_note: "不採用の記録"
origin: "次期マイルストーン追加計画の grilling（2026-07-04）。「engine が稼動する中で発生する複数のイベントに対して hook スクリプトを設定したい。ただしコンセプトから反れる可能性があった場合、計画は中止とする」という条件付き提案"
justifies:
  - "REQ-c1b3ca5f-d2f7-443c-bc4b-b18413ca97b9"
  - "REQ-6c4e174a-4d16-477a-96ff-17cb4eb5b564"
references:
  - "ADR-0003"
  - "ADR-0006"
  - "ADR-0031"
  - "ADR-0033"
---
# ADR-0041: engine イベント hook スクリプト機構を採用しない（合成 idiom を docs に置く）

- ステータス: 採用（不採用の記録）
- 日付: 2026-07-04
- 関連: ADR-0003, ADR-0006, ADR-0031, ADR-0033, `docs/concept.md`, `CONTEXT.md`
- 改訂対象: なし（新規提案の棄却判断）
- 起点: 次期マイルストーン追加計画の grilling（2026-07-04）。「engine が稼動する中で発生する複数のイベントに対して hook スクリプトを設定したい。ただしコンセプトから反れる可能性があった場合、計画は中止とする」という条件付き提案

## 背景

engine の実行イベント（配置前後・stale 除去・世代コミット等）にユーザー定義スクリプトを掛ける hook 機構の提案。home-manager の activation hook や git hooks のような一般的パターンであり、単体では自然な発想だが、提案自体に「コンセプト逸脱なら中止」の条件が付されていたため、コンセプト適合性を正面から評価した。

## 決定

**採用しない。** 評価の結果、提案者の中止条件（コンセプトからの逸脱）に該当すると判断した。

### 判断根拠

1. **「配置フレームワーク」化への一歩になる**。CONTEXT.md は配置プリミティブの定義で「配置フレームワーク」を明示的な _Avoid_ 語としている。engine に任意スクリプト実行のイベント機構を足すことは、「配置という一仕事をする純粋関数」を「ライフサイクルを持つフレームワーク」へ変質させる方向そのもの。
2. **hook の置き場は各層に既にある**。「モジュール = 配線・engine = 配置コア」（ADR-0003）の分離により、配線層が hook ポイントを既に提供している:
   - devShell: `shellHook`（nput apply の前後に任意コマンドを並べられる）
   - home-manager: activation DAG（`lib.hm.dag.entryAfter [ "nput" ] ...` で nput の後に任意ステップ）
   - standalone: UNIX 合成（`nput apply && ./post.sh`。exit code 0/1/2 で分岐可能）
   - 機械可読な実行結果が要る場合: `--json`（ADR-0033）を消費して外部がオーケストレーションする
   engine 内 hook はこれらと重複し、しかも層をバイパスして engine に配線責務を持ち込む。
3. **engine の設計原則と不整合**。engine は `nix` / `git` 以外を叩かない（ADR-0006）・成功時デフォルト沈黙（ADR-0031）・失敗モードは自身のエラー wrap 規約で閉じる、という性質を持つ。任意スクリプトの実行はサブプロセスの失敗・出力・タイムアウトという新しい失敗モード群を engine の意味論に持ち込む。

### 代替: 合成 idiom を docs に 1 節置く

hook が欲しくなる場面（配置後のサービスリロード・通知・後処理）ごとに、上記の層別 idiom（shellHook / activation DAG / shell 合成 + exit code / `--json` 消費）を示す節を docs に追加する。これは機能追加ではなくドキュメント整備であり、本 ADR の帰結として issue 化する。

## 影響

- **docs**: spec.md（または usage ガイド側）に「hook 相当の合成 idiom」節を追加（→ 対応 issue）。ドキュメントサイトの usage ガイド（#146）にも反映する。
- **将来の再提案への基準**: 本 ADR は「engine にイベント機構を持ち込まない」という境界の記録。将来 hook 需要が再燃した場合、まず「どの配線層の idiom で満たせないか」を specific に示すことを再提案の前提条件とする。

## 棄却した代替案

- **engine に pre/post hook を実装する**（原提案）: 上記判断根拠の通り。
- **CLI 層（cmd/nput）に hook を置く**（engine は触らない折衷）: 層は変わっても「nput が任意スクリプトのライフサイクル管理を背負う」点は同じで、shell 合成に対する優位が「設定ファイルに書ける」程度しかない。UNIX 哲学（合成は shell の仕事）に反する。
- **判断を保留して検討継続**: 提案自体が「コンセプト逸脱なら中止」を条件としており、逸脱の根拠が CONTEXT.md の Avoid 語として明文で存在する。保留は判断の先送りにしかならない。
